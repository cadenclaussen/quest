{
    "file-save-hook": {
        "command": "python3 -c \"import json, sys, os; file_path = sys.argv[1]; ext = os.path.splitext(file_path)[1]; [json.dump(json.load(open(file_path)), open(file_path, 'w'), indent=4) if ext in ['.json'] else None, print(f'Reformatted {file_path} with 4-space indentation') if ext in ['.json'] else None]\"",
        "description": "Auto-format JSON files with 4-space indentation on save"
    },
    "task-completion-hook": {
        "command": "osascript -e 'display notification \"Claude Code has finished the task\" with title \"Quest Dev\" sound name \"Glass\"' 2>/dev/null || echo '🎉 Claude Code task completed!' && echo -e '\\a'",
        "description": "Notify when Claude Code completes a task"
    },
    "session-end-hook": {
        "command": "osascript -e 'display notification \"Claude Code session ended\" with title \"Quest Dev\" sound name \"Blow\"' 2>/dev/null || echo '👋 Claude Code session ended!' && echo -e '\\a'",
        "description": "Notify when Claude Code session ends"
    }
}